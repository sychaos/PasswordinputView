<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <TextView
        android:id="@+id/message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_horizontal"
        android:text="@string/pay_message"
        android:textAppearance="@style/text_dark_17" />

    <cloudist.cc.library.view.PasswordInputView
        android:id="@+id/passwordInputView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@color/white"
        app:borderRespondingColor="@color/colorBlue"
        app:borderWidth="0.5dp"
        app:itemHeight="36dp"
        app:itemPadding="8dp"
        app:passwordWidth="6dp" />

    <cloudist.cc.library.view.NumKeyboard
        android:id="@+id/keyboardView"
        android:layout_width="match_parent"
        android:layout_height="202dp"
        android:layout_marginTop="30dp"
        android:orientation="vertical">

    </cloudist.cc.library.view.NumKeyboard>

</LinearLayout>
